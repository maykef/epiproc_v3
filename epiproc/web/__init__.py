"""Web plane: FastAPI app, auth/session/CSRF/MFA, admin, dashboard, health.

Skeleton note: this file tree carries the dashboard TEMPLATE (empty until data
flows) and a minimal bootable app. The full router/auth/admin port from v1's
dashboard_app/api/ lands as the next build step — it is a clean copy-and-own,
rewired from `dashboard_app.api.*` imports to `epiproc.*`.
"""
